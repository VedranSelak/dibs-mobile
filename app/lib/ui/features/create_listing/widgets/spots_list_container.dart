import 'package:flutter/material.dart';

class SpotListContainer extends StatelessWidget {
  const SpotListContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
